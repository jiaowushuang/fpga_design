CMOS探测器的设计步骤

1、初始化
2、寄存器配置
3、输出（位同步：数据和时钟沿对齐、training模式：找到数据的起始位置；相源同步）
   IDDR(串转并：降速)+IO_delay（延迟：在降速）或使用iserdes核
   或是降频：校验数据是否正确（先降一半）

4、跨时钟域：全局时钟控制：DCM/PLL ；数据：信号线（延迟2拍）/总线（FIFO、DDR）；建立时间和保持时间约束

5、SPI时钟与输入时钟的要求：最低比例。


